# Daily Log - 2026-03-23

## Today Goal

- 지난주 작업분을 리뷰하고 GitHub 반영 상태를 정리한다.
- 현재 구현이 실행 가능한지 검증한다.
- 실서버 배포 여부를 확인한다.

## Work Done

- 지난주 이후 `main` 브랜치의 변경 사항과 미커밋 작업을 검토했다.
- 문서 변경과 함께 React/Vite 프론트엔드, Express 백엔드 프록시 골격이 추가된 상태를 확인했다.
- 프론트엔드가 `http://localhost:8787` 를 직접 호출하던 구조를 검토했다.
- 브라우저에서 개발 서버와 API 서버 간 호출이 막힐 수 있는 점을 확인하고 수정했다.
- 프론트엔드 API 호출 경로를 상대 경로 `/api/ask` 로 변경했다.
- Vite dev server에 `/api`, `/health` 프록시를 추가했다.
- `README.md` 에 앱 구조와 로컬 실행 방법을 추가했다.
- `npm install` 을 실행해 의존성을 설치하고 `package-lock.json` 을 생성했다.
- `npm run build` 로 프론트엔드 production build가 통과하는 것을 확인했다.
- Express 서버를 실행해 `/health` 응답을 확인했다.
- `GEMINI_API_KEY` 미설정 상태에서 `/api/ask` 가 예상한 오류를 반환하는지 확인했다.
- 전체 변경을 커밋하고 GitHub `origin/main` 으로 푸시했다.
- 이 서버에 실제 배포 흔적이 있는지 추가 점검했다.
- Nginx, PM2, 일반 배포 디렉터리, systemd 서비스 기준으로 배포 흔적을 확인했지만 찾지 못했다.

## Decisions

- 개발 환경에서는 프론트엔드가 백엔드를 상대 경로로 호출하도록 유지한다.
- Vite proxy를 통해 로컬 개발 시 CORS 이슈를 피한다.
- 오늘 기준 배포 상태는 `GitHub push 완료`, `실서버 배포 미진행` 으로 기록한다.

## Issues and Risks

- 실제 Gemini 연동 end-to-end 테스트는 `GEMINI_API_KEY` 가 없어 아직 수행하지 않았다.
- 현재 서버에는 Nginx, PM2, 배포 디렉터리, 앱 서비스가 없어 운영 배포 상태로 보이지 않는다.
- system-level 일부 확인은 샌드박스 제약이 있어 제한적으로 수행했다.

## Approval Needed

- 실제 Gemini API key 설정
- 실서버 배포를 위한 서비스 등록 또는 웹서버 구성
- 필요 시 포트 오픈, reverse proxy, 운영 배포 권한 변경

## Next Actions

1. `GEMINI_API_KEY` 를 설정해 실제 API 응답까지 end-to-end 검증한다.
2. 실서버 배포 방식을 정한다.
3. 배포 대상이 현재 서버라면 systemd 또는 Nginx 기반 운영 구성을 추가한다.
4. 배포 후 공개 URL 기준으로 실제 화면과 API 동작을 재검증한다.
